services:
  - type: web
    name: hedge-fund-surveillance
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FINNHUB_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
